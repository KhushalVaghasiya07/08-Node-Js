<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blogzyyy - Modern Minimal Blog</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap"
    rel="stylesheet">
  <style>
    /* CSS Variables - Blogzyyy Color Scheme */
    :root {
      --max-width: 1200px;
      --container-padding: 0 2rem;
      --header-height: 70px;
      --font-primary: 'Poppins', 'Segoe UI', system-ui, -apple-system, sans-serif;

      /* Blogzyyy Colors */
      --accent-green: #28b779;
      --accent-orange: #da542e;
      --accent-yellow: #ffb848;
      --accent-green-light: rgba(40, 183, 121, 0.1);
      --accent-orange-light: rgba(218, 84, 46, 0.1);
      --accent-yellow-light: rgba(255, 184, 72, 0.1);

      --color-text: #232e3c;
      --color-text-light: #454552;
      --color-text-lighter: #adb6bd;
      --color-bg-light: #f9fafb;
      --color-border: #e9eaea;
      --shadow-sm: 0 1px 3px rgba(52, 64, 74, 0.1);
      --shadow-md: 0 4px 6px rgba(52, 64, 74, 0.1);
      --radius: 8px;
      --radius-lg: 12px;
      --transition: all 0.3s ease;
    }

    /* Reset and Base Styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-primary);
      color: var(--color-text);
      line-height: 1.6;
      background-color: #fff;
    }

    .container {
      max-width: var(--max-width);
      margin: 0 auto;
      padding: var(--container-padding);
    }

    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    a {
      text-decoration: none;
      color: inherit;
      transition: var(--transition);
    }

    /* Header/Navbar - Blogzyyy Style */
    header {
      height: var(--header-height);
      display: flex;
      align-items: center;
      background: #fff;
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: var(--shadow-sm);
      border-bottom: 1px solid var(--color-border);
    }

    .header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      width: 1140px;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 800;
      background: linear-gradient(90deg, var(--accent-orange) 0%, var(--accent-yellow) 50%, var(--accent-green) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .logo span {
      font-size: 0.7rem;
      background: var(--accent-green-light);
      color: var(--accent-green);
      padding: 2px 8px;
      border-radius: 12px;
      margin-left: 8px;
      font-weight: 600;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 2rem;
    }

    .nav-link {
      position: relative;
      font-weight: 500;
      color: var(--color-text-light);
    }

    .nav-link::after {
      content: '';
      position: absolute;
      width: 0;
      height: 3px;
      bottom: -5px;
      left: 0;
      background: linear-gradient(90deg, var(--accent-orange) 0%, var(--accent-yellow) 50%, var(--accent-green) 100%);
      transition: width 0.3s ease;
    }

    .nav-link:hover {
      color: var(--color-text);
    }

    .nav-link:hover::after {
      width: 100%;
    }

    .btn {
      padding: 0.6rem 1.2rem;
      border-radius: 6px;
      font-weight: 500;
      transition: var(--transition);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      border: none;
      font-family: inherit;
    }

    .btn-primary {
      background: linear-gradient(to right, var(--accent-orange), var(--accent-yellow));
      color: white;
      box-shadow: 0 4px 12px rgba(218, 84, 46, 0.25);
    }

    .btn-primary:hover {
      box-shadow: 0 6px 15px rgba(218, 84, 46, 0.35);
      transform: translateY(-2px);
    }

    .btn-outline {
      border: 1px solid var(--accent-green);
      color: var(--accent-green);
      background: transparent;
    }

    .btn-outline:hover {
      background: var(--accent-green-light);
    }

    .btn-sm {
      padding: 0.4rem 0.8rem;
      font-size: 0.875rem;
    }

    /* Blog Content */
    .blog-header {
      padding: 3rem 0 2rem;
    }

    .blog-title {
      font-size: 2.25rem;
      font-weight: 800;
      line-height: 1.2;
      margin-bottom: 1rem;
      color: var(--color-text);
    }

    .blog-meta {
      display: flex;
      align-items: center;
      gap: 1rem;
      color: var(--color-text-light);
      font-size: 0.875rem;
      margin-bottom: 1.5rem;
    }

    .author {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .author-img {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      object-fit: cover;
    }

    .featured-image {
      width: 100%;
      height: auto;
      max-height: 450px;
      object-fit: cover;
      border-radius: var(--radius-lg);
      margin: 2rem 0;
      box-shadow: var(--shadow-md);
    }

    /* Article Content */
    .article-content {
      padding: 2rem 0;
      font-size: 1.125rem;
      line-height: 1.7;
    }

    .article-content p {
      margin-bottom: 1.5rem;
      color: var(--color-text);
    }

    .article-content h2 {
      font-size: 1.375rem;
      font-weight: 700;
      margin: 2.5rem 0 1rem;
      color: var(--color-text);
    }

    .article-content h3 {
      font-size: 1.25rem;
      font-weight: 600;
      margin: 2rem 0 1rem;
      color: var(--color-text);
    }

    .article-content blockquote {
      border-left: 4px solid var(--accent-green);
      padding-left: 1.5rem;
      margin: 2rem 0;
      font-style: italic;
      color: var(--color-text-light);
      background-color: var(--accent-green-light);
      padding: 1.5rem;
      border-radius: 0 var(--radius) var(--radius) 0;
    }

    /* Tags */
    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      margin: 2rem 0;
    }

    .tag {
      padding: 0.5rem 1rem;
      background: var(--color-bg-light);
      border-radius: 2rem;
      font-size: 0.875rem;
      color: var(--color-text-light);
      transition: var(--transition);
    }

    .tag:hover {
      background: var(--accent-green-light);
      color: var(--accent-green);
    }

    /* Author Info */
    .author-card {
      background: var(--color-bg-light);
      padding: 1.5rem;
      border-radius: var(--radius);
      margin: 3rem 0;
      display: flex;
      gap: 1.5rem;
      align-items: center;
    }

    .author-card-img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      object-fit: cover;
    }

    .author-details h3 {
      margin-bottom: 0.5rem;
      font-size: 1.25rem;
    }

    .author-details p {
      color: var(--color-text-light);
      margin-bottom: 1rem;
    }

    .social-links {
      display: flex;
      gap: 1rem;
    }

    .social-links a {
      color: var(--color-text-lighter);
      font-size: 1.25rem;
    }

    .social-links a:hover {
      color: var(--accent-green);
    }

    /* Comments */
    .comments-section {
      margin: 3rem 0;
    }

    .section-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      color: var(--color-text);
    }

    .comment {
      background: white;
      padding: 1.5rem;
      border-radius: var(--radius);
      box-shadow: var(--shadow-sm);
      margin-bottom: 1.5rem;
      border: 1px solid var(--color-border);
    }

    .comment-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .comment-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      object-fit: cover;
    }

    .comment-meta {
      flex-grow: 1;
    }

    .comment-author {
      font-weight: 600;
      margin-bottom: 0.25rem;
      color: var(--color-text);
    }

    .comment-date {
      color: var(--color-text-light);
      font-size: 0.875rem;
    }

    .comment-content {
      color: var(--color-text);
      line-height: 1.6;
    }

    /* Related Posts */
    .related-posts {
      margin: 3rem 0;
    }

    .section-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    .posts-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .post-card {
      background: white;
      border-radius: var(--radius);
      overflow: hidden;
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
      border: 1px solid var(--color-border);
      display: flex;
      flex-direction: column;
    }

    .post-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
    }

    .post-image {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .post-content {
      padding: 1.5rem;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
    }

    .post-category {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      background: var(--accent-green-light);
      color: var(--accent-green);
      border-radius: 2rem;
      font-size: 0.75rem;
      font-weight: 500;
      margin-bottom: 0.75rem;
      align-self: flex-start;
    }

    .post-title {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      line-height: 1.4;
      color: var(--color-text);
    }

    .post-excerpt {
      color: var(--color-text-light);
      font-size: 0.875rem;
      margin-bottom: 1rem;
      line-height: 1.5;
      flex-grow: 1;
    }

    .post-meta {
      display: flex;
      align-items: center;
      justify-content: space-between;
      color: var(--color-text-lighter);
      font-size: 0.75rem;
      margin-bottom: 1rem;
    }

    .post-actions {
      margin-top: auto;
    }

    .view-all-container {
      text-align: center;
      margin-top: 2rem;
    }

    /* Footer */
    footer {
      background: #1a202c;
      color: white;
      padding: 3rem 0;
    }

    .footer-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 2rem;
    }

    .footer-col h4 {
      font-size: 1.125rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      color: white;
    }

    .footer-links {
      list-style: none;
    }

    .footer-links li {
      margin-bottom: 0.75rem;
    }

    .footer-links a {
      color: #cbd5e0;
    }

    .footer-links a:hover {
      color: white;
    }

    .copyright {
      text-align: center;
      margin-top: 3rem;
      padding-top: 1.5rem;
      border-top: 1px solid #2d3748;
      color: #a0aec0;
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
      .posts-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .footer-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 768px) {
      .header-content {
        flex-direction: column;
        gap: 1rem;
        padding: 1rem 0;
        height: auto;
        min-height: var(--header-height);
      }

      nav ul {
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
      }

      .blog-title {
        font-size: 1.875rem;
      }

      .author-card {
        flex-direction: column;
        text-align: center;
      }

      .posts-grid {
        grid-template-columns: 1fr;
      }

      .footer-grid {
        grid-template-columns: 1fr;
        gap: 2rem;
      }

      .section-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }
    }

    @media (max-width: 640px) {
      :root {
        --container-padding: 0 1rem;
      }

      .blog-meta {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
      }
    }
  </style>
</head>

<body>
  <!-- Header/Navbar -->
  <header>
    <div class="container">
      <div class="header-content">
        <a href="/Blogzyyy" class="logo">Blogzyyy<span>BETA</span></a>
        <nav>
          <ul>
            <li><a href="#" class="nav-link">Home</a></li>
            <li><a href="#" class="nav-link">Discover</a></li>
            <li><a href="#" class="nav-link">About</a></li>
            <li><a href="#" class="nav-link">Contact</a></li>
          </ul>
        </nav>
        <div>
          <a href="#" class="btn btn-primary">Sign In</a>
        </div>
      </div>
    </div>
  </header>

  <main class="container">
    <!-- Blog Header -->
    <section class="blog-header">
      <h1 class="blog-title">
        <%= blogs.title %>
      </h1>
      <div class="blog-meta">
        <div class="author">
          <img
            src="<%= blogs.authImg || 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?ixlib=rb-4.0.3&auto=format&fit=crop&w=100&h=100&q=80' %>"
            alt="Author" class="author-img">
          <span>
            <%= blogs.authname %>
          </span>
        </div>
        <span>
          <%= new Date(blogs.createdAt).toLocaleDateString('en-US', { year: 'numeric' , month: 'long' , day: 'numeric'
            }) %>
        </span>
        <span>
          <%= Math.ceil(blogs.description.split(' ').length / 200) %> min read
        </span>
      </div>
    </section>

    <!-- Featured Image -->
    <img src="<%= blogs.blogImg %>"
            alt="Featured Image" class="featured-image">

            <!-- Article Content -->
            <article class="article-content">
              <% if(blogs.description) { %>
                <% let paragraphs=blogs.description.split('\n\n'); %>
                  <% paragraphs.forEach(paragraph=> { %>
                    <% if(paragraph.trim() !=='' ) { %>
                      <p>
                        <%= paragraph %>
                      </p>
                      <% } %>
                        <% }); %>
                          <% } else { %>
                            <p class="text-slate-400 italic">No content available for this blog post.</p>
                            <% } %>
            </article>

            <!-- Tags -->
            <div class="tags">
              <span class="tag">
                <%= blogs.category %>
              </span>
              <span class="tag">Web Development</span>
              <span class="tag">Technology</span>
              <span class="tag">Trending</span>
            </div>

            <!-- Author Info -->
            <div class="author-card">
              <img
                src="<%= blogs.authImg || 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?ixlib=rb-4.0.3&auto=format&fit=crop&w=160&h=160&q=80' %>"
                alt="Author" class="author-card-img">
              <div class="author-details">
                <h3>
                  <%= blogs.authname %>
                </h3>
                <p>
                  <%= blogs.authname %> is a passionate writer who loves to share insights about
                    <%= blogs.category %> and related topics. With expertise in the field, they aim to provide valuable
                      content to
                      the readers.
                </p>
                <div class="social-links">
                  <a href="#"><i class="fab fa-twitter"></i></a>
                  <a href="#"><i class="fab fa-linkedin"></i></a>
                  <a href="#"><i class="fab fa-github"></i></a>
                  <a href="#"><i class="fab fa-medium"></i></a>
                </div>
              </div>
            </div>

            <!-- Related Posts -->
            <section class="related-posts">
              <div class="section-header">
                <h2 class="section-title">Latest Blogs</h2>
                <a href="/blog/viewallblog" class="btn btn-outline">View All</a>
              </div>

              <div class="posts-grid">
                <% if(latestBlogs && latestBlogs.length> 0) { %>
                  <% latestBlogs.forEach(blog=> { %>
                    <article class="post-card">
                      <img
                        src="<%= blog.blogImg %>"
                        alt="Related post" class="post-image">
                      <div class="post-content">
                        <span class="post-category">
                          <%= blog.category %>
                        </span>
                        <h3 class="post-title">
                          <%= blog.title %>
                        </h3>
                        <p class="post-excerpt">
                          <%= blog.description.substring(0, 100) %>...
                        </p>
                        <div class="post-meta">
                          <span>
                            <%= new Date(blog.createdAt).toLocaleDateString('en-US', { month: 'short' , day: 'numeric'
                              }) %>
                          </span>
                          <span>
                            <%= Math.ceil(blog.description.split(' ').length / 200) %> min read
                  </span>
                </div>
                <div class="post-actions">
                  <a href="/Blogzyyy/blogs/<%= blog._id %>" class="btn btn-sm" style="color:  #28b779;">
                    Read More
                    <i class="fas fa-arrow-right" style="margin-left: 6px; font-size: 0.75rem;"></i>
                  </a>
                </div>
              </div>
            </article>
            <% }); %>
              <% } else { %>
                <p>No related posts available.</p>
                <% } %>
      </div>


    </section>
  </main>

  <!-- Footer -->
  <footer>
    <div class="container">
      <div class="footer-grid">
        <div class="footer-col">
          <h4>Blogzyyy</h4>
          <p style="color: #cbd5e0; margin-bottom: 1rem;">The ultimate platform for Gen-Z to express and explore ideas.
          </p>
          <div class="social-links">
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-tiktok"></i></a>
            <a href="#"><i class="fab fa-discord"></i></a>
          </div>
        </div>

        <div class="footer-col">
          <h4>Explore</h4>
          <ul class="footer-links">
            <li><a href="#">Home</a></li>
            <li><a href="#">Blogs</a></li>
            <li><a href="#">Categories</a></li>
            <li><a href="#">About Us</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Community</h4>
          <ul class="footer-links">
            <li><a href="#">Join Discord</a></li>
            <li><a href="#">Author Guidelines</a></li>
            <li><a href="#">Community Standards</a></li>
            <li><a href="#">Careers</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4>Get in Touch</h4>
          <ul class="footer-links">
            <li><a href="#">Contact Us</a></li>
            <li><a href="#">Support</a></li>
            <li><a href="#">Feedback</a></li>
            <li><a href="#">Advertise</a></li>
          </ul>
        </div>
      </div>

      <div class="copyright">
        <p>© 2025 Blogzyyy. All rights reserved. Made with 💚 for the digital generation.</p>
      </div>
    </div>
  </footer>

  <script>
    // Simple reading progress indicator
    window.addEventListener(' scroll', () => {
      const winHeight = window.innerHeight;
      const docHeight = document.documentElement.scrollHeight;
      const scrollTop = window.pageYOffset;
      const trackLength = docHeight - winHeight;
      const progress = (scrollTop / trackLength) * 100;

      // You could use this to update a progress bar if needed
      console.log(`Reading progress: ${Math.round(progress)}%`);
    });
  </script>
</body>

</html>